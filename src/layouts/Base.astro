---
import Nav from '../components/Nav.astro';

export interface Props {
  title?: string;
}

const { title = "My Site" } = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  </head>

  <body class="min-h-screen bg-zinc-950 text-zinc-100">
    <header class="max-w-3xl mx-auto px-4">
      <Nav />
    </header>

    <main class="max-w-3xl mx-auto px-4 mt-8">
      <slot />
    </main>

    <footer class="max-w-3xl mx-auto px-4 mt-12 mb-8 text-xs text-zinc-500">
      © {new Date().getFullYear()} HanBin Baik
    </footer>
  </body>
</html>
